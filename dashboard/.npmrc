# This 2019-era dependency tree has peer-dependency conflicts that npm 7+
# rejects by default (e.g. image-webpack-loader@4 wants webpack-cli@^2 while
# the project uses webpack-cli@3). Accept the existing resolution so a plain
# `npm install` works on modern Node/npm without extra flags.
legacy-peer-deps=true
