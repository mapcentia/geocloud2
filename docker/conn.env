# Default connection settings for Dev Container and services
# Used by gc2core and event services via env_file in docker-compose.yml

# Postgres (PostGIS) connection
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=gc2
POSTGRES_PASSWORD=1234
POSTGRES_PGBOUNCER=false

# Optional: Redis connection (if read by app/services in the future)
# REDIS_HOST=redis
# REDIS_PORT=6379
