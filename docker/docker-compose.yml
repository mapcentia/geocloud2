version: '3.3'

networks:
  internal:
    external: false

services:
  base:
    build: ./base
    image: mapcentia/gc2core8:base
  ecw:
    build: ./ecw
    image: mapcentia/gc2core8:ecw
    depends_on:
      - base
  gdal:
    build: ./gdal
    image: mapcentia/gc2core8:gdal
    depends_on:
      - ecw
  mapserver:
    build: ./mapserver
    image: mapcentia/gc2core8:mapserver
    depends_on:
      - gdal
  gc2core:
    build: ./stable
    image: mapcentia/gc2core8
    depends_on:
      - mapserver
    privileged: true
    volumes:
      - ../../geocloud2:/var/www/geocloud2
    networks:
      - default
      - internal
    ports:
      - "8080:80"
    expose:
      - 9000
    environment:
      - POSTGIS_HOST=postgis
      - POSTGIS_DB=postgres
      - POSTGIS_USER=gc2
      - POSTGIS_PORT=5432
      - POSTGIS_PW=1234
      - POSTGIS_PGBOUNCER=false
      - MODE_ENV=${MODE_ENV}
      - BUILD_ID=${BUILD_ID}
    extra_hosts:
      - host.docker.internal:host-gateway
  redis:
    image: redis
    networks:
      - default
      - internal
    ports:
      - "6379:6379"
  memcached:
    image: memcached
    networks:
      - default
      - internal
    ports:
      - "11211:11211"
  postgis:
    build: ./postgis
    image: mapcentia/postgis15
    volumes:
      - postgis_var_lib_postgresql:/var/lib/postgresql
    networks:
      - default
      - internal
    ports:
      - "5432:5432"
    environment:
      - GC2_USER=gc2
      - GC2_PASSWORD=1234

#  limesurvey:
#    image: acspri/limesurvey
#    ports:
#      - 8082:80
#    networks:
#      - default
#      - internal
#    environment:
#      LIMESURVEY_DB_PASSWORD: 1234
#      LIMESURVEY_DB_USER: gc2
#      LIMESURVEY_DB_HOST: postgis
#      LIMESURVEY_DB_NAME: mydb
#      LIMESURVEY_ADMIN_USER: admin
#      LIMESURVEY_ADMIN_PASSWORD: password
#      LIMESURVEY_ADMIN_NAME: Lime Administrator
#      LIMESURVEY_ADMIN_EMAIL: lime@lime.lime
#    volumes:
#      - ./plugins:/var/www/html/plugins
#      - ./upload:/var/www/html/upload
#      - ./config:/var/www/html/application/config

#  elasticsearch:
#    image: elasticsearch:7.17.6
#    networks:
#      - default
#      - internal
#    ports:
#      - "9200:9200"
#    environment:
#      - "discovery.type=single-node"

volumes:
  postgis_var_lib_postgresql:





